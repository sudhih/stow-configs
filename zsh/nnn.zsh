# default `nnn` option
  # -E internal edits in EDITOR
  # -e text in $VISUAL/$EDITOR
  # -n type-to-nav mode
  # -r use advanced cp, mv patch
export NNN_OPTS="er"

# key-plugin pairs
export NNN_PLUG='f:finder;o:fzopen;j:autojump'
